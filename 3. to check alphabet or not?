#include <stdio.h>

int main() { 

     char ch;
     printf("enter a alphabet:");
     scanf("%c",&ch);
     ch >= 'a' && ch <= 'z' || ch >= 'A' && ch <= 'Z' ? printf("it is a alphabet \n"): printf("not a alphabet \n");
    return 0;
}
